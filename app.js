function askname() {
    var name = prompt("What is your name?")
    return alert(`Hello ${name}`)
}

alert(askname())

function convert() {
    var conv = prompt("Would you like to convert from `c` or `f`")

}

function temperature() {
    var temp = prompt("Enter a temperature")
}

alert(temperature())